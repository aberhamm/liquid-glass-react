# @aberhamm/liquid-glass-react

An independent React reimplementation of a liquid-glass UI component library, shipping ESM + CJS bundles with TypeScript declarations. This is the scaffold release (placeholder export only); components, styles, and the public prop API arrive in later milestones. Full usage docs land in plan 011.
